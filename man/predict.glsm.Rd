% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.glsm.R
\name{predict.glsm}
\alias{predict.glsm}
\title{Predict method for GLSM objects}
\usage{
\method{predict}{glsm}(object, newdata = NULL, type = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{An object of class \code{glsm} returned by \code{\link[=glsm]{glsm()}}.}

\item{newdata}{An optional data frame in which to look for variables with which to predict.
Currently, passing \code{newdata} is not supported.}

\item{type}{Type of prediction to compute. One of \code{"link"} (linear predictor),
\code{"response"} (probabilities), \code{"odd"} (odds), or \code{"OR"} (odds ratios). Defaults to \code{"response"}.}

\item{level}{Confidence level for the linear predictor interval. Only used if \code{type = "link"}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A list with predictions and confidence intervals if \code{type = "link"}, otherwise a matrix of predicted values.
}
\description{
Computes predictions from a fitted \code{glsm} model. Supported prediction types include
linear predictors (link), fitted probabilities (response), odds, and odds ratios (OR).
}
\details{
The function returns the requested type of prediction:
\itemize{
\item \code{"link"}: Linear predictors with confidence intervals.
\item \code{"response"}: Predicted class probabilities.
\item \code{"odd"}: Estimated odds.
\item \code{"OR"}: Odds ratios relative to the reference category.
}
}
\seealso{
\code{\link[=glsm]{glsm()}}
}
